language: ruby
rvm:
 - 2.3
cache: 
  - bundler
services:
  - postgresql
addons:
  postgresql: 
    - "9.6"
before_script:
  - psql -c 'create database ink_stream_test;' -U postgres
  - bundle exec rake db:create
  - bundle exec rake db:migrate
script: 
  - bundle exec rspec  